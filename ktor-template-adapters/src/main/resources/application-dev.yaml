ktor:
  deployment:
    host: 0.0.0.0
    port: 8080
  application:
    modules:
      - com.yonatankarp.ktor.template.ApplicationKt.module
  environment: dev

database:
  url: jdbc:postgresql://localhost:5432/ktor-template
  username: postgres
  password: secret
  maximumPoolSize: 10
