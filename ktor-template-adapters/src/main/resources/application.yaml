ktor:
  deployment:
    host: 0.0.0.0
    port: 8080
  application:
    modules:
      - com.yonatankarp.ktor.template.ApplicationKt.module
  environment: ${KTOR_ENV:prod}

database:
  url: ${DB_URL}
  username: ${DB_USERNAME}
  password: ${DB_PASSWORD}
  maximumPoolSize: ${DB_POOL_SIZE:10}
