// Root project. Module-level configuration lives in each subproject's build.gradle.kts.
// Cross-cutting conventions are in buildSrc/ as `ktor-template.*` plugins.

group = "com.yonatankarp"
version = "0.1.0"
